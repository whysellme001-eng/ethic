/**
 * ─────────────────────────────────────────────────────────────
 *  EDIT ME — site customization
 * ─────────────────────────────────────────────────────────────
 *
 *  downloadUrl: where the "Download" buttons point.
 *    - Paste a direct link to your .zip / .crx, a GitHub release,
 *      a Chrome Web Store listing, or any URL you want.
 *
 *  logoSrc: the logo shown in the nav and hero.
 *    - Drop an image in the /public folder (e.g. /public/my-logo.png)
 *      and set this to "/my-logo.png".
 *    - Or paste any full image URL (https://...).
 *
 *  Everything else below is optional branding text.
 */
export const siteConfig = {
  name: "Ethic",
  tagline: "Browser extension for MP4 sample-count patching",

  // 👇 Change this to your real download link
  downloadUrl: "https://example.com/ethic-extension.zip",

  // 👇 Change this to your logo (public path or full URL)
  logoSrc: "/ethic-logo.png",

  // Optional: shown on the download button. e.g. "v1.0.0"
  version: "v1.0.0",
}
